
#include "Menu.h"
#include "BinomialHeap.h"
int main() {
    menu();
    /*// Creating a new BinomialHeap object
    BinomialHeap binomialHeap;

    // Inserting some keys into the heap
    binomialHeap.insertKey(10.2);
    binomialHeap.insertKey(5.4);
    binomialHeap.insertKey(20.9);
    binomialHeap.insertKey(15.21);
    binomialHeap.insertKey(25.1);

    // Printing the heap before performing any operations
    std::cout << "Heap before operations:" << std::endl;
    binomialHeap.print();
    std::cout << std::endl;

    // Extracting the minimum key
    std::cout << "Extracting the minimum key:" << std::endl;
    binomialHeap.extractMin();
    binomialHeap.print();
    std::cout << std::endl;

    // Decreasing the key of a node
    std::cout << "Decreasing the key of a node:" << std::endl;
    Node* nodeToDecrease = binomialHeap.findMin();
    binomialHeap.decreaseKey(3.8, nodeToDecrease);
    binomialHeap.print();
    std::cout << std::endl;

    // Deleting a node with a specific key
    std::cout << "Deleting a node with a specific key:" << std::endl;
    binomialHeap.deleteKey(nodeToDecrease->sibling->child);
    binomialHeap.print();
    std::cout << std::endl;*/
    return 0;
}
